import { Pizza } from "./Pizza";

export let price = (pizza: Pizza): number => {
    let cost = 0;

    // Base Price
    // - small: $7
    // - medium: $9
    // - large: $11

    // Extra cheese adds $1

    // Each topping adds $0.75

    return cost;
};