/**
 * This class represents a basic Pizza order.
 * 
 * Notice it is exported and thus available for other
 * files to import.
 */
export class Pizza {

    // Expected values are "small", "medium", or "large"
    size: string = "medium";

    extraCheese: boolean = false;

    toppings: number = 0;
    
}