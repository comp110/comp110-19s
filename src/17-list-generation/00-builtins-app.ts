import { first, rest, cons, listify } from "introcs/list";

import { print } from "introcs";

export let main = async () => {
    // TODO: An example of the listify function

    // TODO: Print the first and rest of the list produced
    
};

main();