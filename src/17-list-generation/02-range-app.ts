import { first, rest, cons, listify, Node } from "introcs/list";

import { print } from "introcs";

export let main = () => {
    let numbers = range(2, 9);
    print(numbers);
};

let range = (lo: number, hi: number): Node<number> => {
    return null;
};

main();