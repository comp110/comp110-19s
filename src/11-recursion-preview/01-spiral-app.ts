import { forward, left, setSpeed } from "introcs/turtle";

export let main = async () => {
    setSpeed(0.3);
    spiral(100);
};

/**
 * Draws a spiral in terms of itself and `forwardTurn`.
 * It should result in a spiral of `width` pixels.
 */
let spiral =  (width: number): void => {
    // TODO
};

main();