import { forward, left, setSpeed } from "introcs/turtle";

export let main = async () => {
    setSpeed(0.3);
    // TODO #3 - Call square with a width of 100.
};

/**
 * Moves the turtle forward by the parameter 
 * `distance` amount and then turns the
 * turtle left by 90 degrees.
 */
let forwardTurn = (distance: number): void => {
    // TODO #1
};

/**
 * Draws a square in terms of `forwardTurn` 
 * `forwardTurn`. It should result in a square of
 * `width` pixels.
 */
let square =  (width: number): void => {
    // TODO #2
};

main();